apiVersion: batch/v1
kind: Job
metadata:
  name: productor-dlq
spec:
  template:
    spec:
      containers:
        - name: productor
          image: productor-dlq:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: RABBITMQ_HOST
              value: rabbitmq
      restartPolicy: Never