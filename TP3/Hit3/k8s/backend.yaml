apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: sobel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      nodeSelector:
        nodegroup: apps
      tolerations:
        - key: nodegroup
          value: apps
          effect: NoSchedule
      initContainers:
        - name: wait-rabbitmq
          image: busybox
          command:
            - sh
            - -c
            - until nc -z rabbitmq 5672; do echo "Esperando RabbitMQ..."; sleep 3; done
        - name: wait-redis
          image: busybox
          command:
            - sh
            - -c
            - until nc -z redis 6379; do echo "Esperando Redis..."; sleep 3; done
      containers:
        - name: backend
          image: gcr.io/mi-proyecto/sobel-backend:latest
          ports:
            - containerPort: 8080
          env:
            - name: RABBITMQ_HOST
              value: "rabbitmq"
            - name: RABBITMQ_USER
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secret
                  key: user
            - name: RABBITMQ_PASS
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secret
                  key: pass
            - name: REDIS_HOST
              value: "redis"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: sobel
spec:
  type: LoadBalancer
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 8080
